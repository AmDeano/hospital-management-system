# api-gateway/Dockerfile
FROM nginx:alpine

# Copy custom nginx configuration
COPY nginx-api-gateway.conf /etc/nginx/nginx.conf

# Create cache directory
RUN mkdir -p /var/cache/nginx

# Expose port 80
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]